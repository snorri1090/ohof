%h1 Out of Home Office, Tacoma
%p The anxiety is coming from INSIDE THE HOUSE!

= link_to('Add a New Visit', visits_new_path, role: "button", class: "btn btn-primary")

%h2 New great place to work?
= link_to("Add a New Location", locations_new_path, role: "button", class: "btn btn-primary")
